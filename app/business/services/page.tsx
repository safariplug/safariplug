import { redirect } from "next/navigation";
import Link from "next/link";
import { createSupabaseServerClient } from "@/lib/supabase-server";
import { supabaseAdmin } from "@/lib/supabase-admin";
import ProviderWorkspace from "./ProviderWorkspace";
import ServiceProviderTerms from "./ServiceProviderTerms";
export const dynamic = "force-dynamic";
export default async function BusinessServicesDashboard(){const supabase=await createSupabaseServerClient();const {data:{user}}=await supabase.auth.getUser();if(!user||user.is_anonymous||!(user.email_confirmed_at||user.phone_confirmed_at))redirect("/login?next=/business/services");const {data:businesses}=await supabaseAdmin.from("businesses").select("id,name,slug,description,business_type,status,verified,claimed,phone,whatsapp,email").eq("owner_id",user.id).in("status",["active","ACTIVE"]).order("created_at",{ascending:true}).limit(1);const business=businesses?.[0]??null;const {data:categories}=await supabaseAdmin.from("service_categories").select("id,name,slug,description").eq("status","active").order("name");let profile:any=null,category:any=null,offerings:any[]=[],staff:any[]=[],appointments:any[]=[];if(business){const {data}=await supabaseAdmin.from("service_profiles").select("id,business_id,category_id,status,booking_status,timezone,cancellation_policy,booking_notice_minutes,max_booking_days,provider_terms_version,provider_terms_accepted_at,provider_terms_accepted_by,service_fee_percent,service_categories(id,name,slug,description)").eq("business_id",business.id).maybeSingle();profile=data;category=profile?.service_categories??null;if(profile){const [{data:o},{data:s},{data:a}]=await Promise.all([supabaseAdmin.from("service_offerings").select("id,name,description,duration_minutes,price,currency,status,requires_confirmation").eq("service_profile_id",profile.id).order("created_at",{ascending:true}),supabaseAdmin.from("service_staff").select("id,display_name,bio,status").eq("service_profile_id",profile.id).order("created_at",{ascending:true}),supabaseAdmin.from("service_appointments").select("id,public_id,customer_name,starts_at,ends_at,status,price,currency,payment_status,payment_reference,paid_at,service_offerings(name),service_staff(display_name)").eq("service_profile_id",profile.id).order("starts_at",{ascending:true}).limit(50)]);offerings=o??[];staff=s??[];appointments=a??[];}}return <main className="min-h-screen bg-[#f7f7f4] text-[#111]"><header className="border-b border-black/8 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 sm:px-10"><div><Link href="/" className="text-sm font-semibold tracking-tight">SafariPlug</Link><p className="mt-1 text-[10px] font-semibold uppercase tracking-[.25em] text-black/35">Partner workspace</p></div><div className="flex items-center gap-3">{business&&<span className={`rounded-full px-3 py-1.5 text-[11px] font-semibold ${business.verified?"bg-emerald-50 text-emerald-700":"bg-amber-50 text-amber-700"}`}>{business.verified?"Verified business":"Verification pending"}</span>}{business&&<Link href="/business/payouts" className="rounded-xl bg-black px-4 py-2 text-xs font-semibold text-white">Earnings & payouts</Link>}{business&&<Link href={`/services/${business.slug}`} className="rounded-xl border border-black/10 px-4 py-2 text-xs font-semibold">View storefront ↗</Link>}</div></div></header><section className="mx-auto max-w-7xl space-y-6 px-6 py-10 sm:px-10">{business&&profile&&<ServiceProviderTerms businessId={business.id} profileId={profile.id} acceptedAt={profile.provider_terms_accepted_at} feePercent={Number(profile.service_fee_percent||10)}/>}<ProviderWorkspace business={business} profile={profile} category={category} categories={categories??[]} offerings={offerings} staff={staff} appointments={appointments}/></section></main>;
}
