"use client";

import { useEffect, useState } from "react";

const stages = ["pending", "accepted", "preparing", "ready", "driver_assigned", "picked_up", "on_the_way", "delivered"];
const labels: Record<string,string> = { pending:"Placed", accepted:"Accepted", preparing:"Preparing", ready:"Ready", driver_assigned:"Driver assigned", picked_up:"Picked up", on_the_way:"On the way", delivered:"Delivered", cancelled:"Cancelled", rejected:"Rejected" };

type Order = { id:string; public_id?:string|null; status:string; payment_status:string; fulfillment_method:string; currency:string; customer_total:number; delivery_address?:string|null; eta_at?:string|null; created_at:string; businesses?:{name?:string|null}|null; food_order_items?:{item_name:string;quantity:number;line_total:number}[]; food_delivery_assignments?:{status:string;driver_id?:string|null;delivery_fee?:number|null}[] };

type Driver = { id:string; display_name?:string|null; service_city?:string|null; service_country?:string|null };

export default function OrdersPage(){
 const [orders,setOrders]=useState<Order[]>([]); const [drivers,setDrivers]=useState<Record<string,Driver>>({}); const [message,setMessage]=useState("Loading your orders…"); const [busy,setBusy]=useState("");
 async function load(){const [r,dr]=await Promise.all([fetch("/api/restaurants/orders/status",{cache:"no-store"}),fetch("/api/restaurants/drivers",{cache:"no-store"})]);const d=await r.json();if(!r.ok){setMessage(d.error||"Unable to load orders.");return}setOrders(d.orders||[]);if(dr.ok){const dd=await dr.json();setDrivers(Object.fromEntries((dd.drivers||[]).map((x:Driver)=>[x.id,x])))}setMessage("")}
 useEffect(()=>{void load();const id=setInterval(()=>void load(),15000);return()=>clearInterval(id)},[]);
 async function cancel(id:string){setBusy(id);try{const r=await fetch("/api/restaurants/orders/status",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({orderId:id,status:"cancelled",note:"Cancelled by customer"})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Unable to cancel order");await load()}catch(e:any){setMessage(e.message)}finally{setBusy("")}}
 return <main className="min-h-screen bg-slate-50 px-5 py-10"><div className="mx-auto max-w-4xl"><p className="text-xs font-bold uppercase tracking-[0.25em] text-amber-700">SafariPlug Dining</p><h1 className="mt-2 text-4xl font-semibold">Your orders</h1><p className="mt-2 text-slate-600">Track restaurant orders from payment through delivery.</p>{message&&<p className="mt-6 rounded-2xl bg-white p-4 text-sm text-slate-600">{message}</p>}<div className="mt-8 space-y-5">{orders.map(o=>{const assignment=o.food_delivery_assignments?.find(a=>a.status!=="cancelled"&&a.status!=="declined");const driver=assignment?.driver_id?drivers[assignment.driver_id]:undefined;return <article key={o.id} className="rounded-3xl border bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-semibold">{o.businesses?.name||"Restaurant"}</p><h2 className="mt-1 text-xl font-semibold">Order {o.public_id||o.id}</h2><p className="mt-1 text-sm capitalize text-slate-500">{o.fulfillment_method.replaceAll("_"," ")} · Payment: {o.payment_status}</p></div><div className="text-right"><p className="font-semibold">{o.currency} {Number(o.customer_total).toLocaleString()}</p>{o.eta_at&&<p className="mt-1 text-xs text-slate-500">ETA {new Date(o.eta_at).toLocaleString()}</p>}</div></div><div className="mt-5 grid grid-cols-4 gap-1 sm:grid-cols-8">{stages.map((s,i)=>{const current=stages.indexOf(o.status);const active=current>=i;return <div key={s} className="text-center"><div className={`mx-auto h-2 rounded-full ${active?"bg-black":"bg-slate-200"}`}/><p className={`mt-2 text-[10px] leading-3 ${active?"font-semibold text-slate-900":"text-slate-400"}`}>{labels[s]}</p></div>})}</div>{driver&&<div className="mt-5 rounded-2xl bg-slate-50 p-4"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Delivery driver</p><p className="mt-1 font-semibold">{driver.display_name||"SafariPlug driver"}</p>{driver.service_city&&<p className="text-sm text-slate-500">{driver.service_city}{driver.service_country?`, ${driver.service_country}`:""}</p>}<p className="mt-2 text-xs text-slate-500">Driver status: {assignment?.status?.replaceAll("_"," ")}</p></div>}{o.delivery_address&&<p className="mt-4 text-sm text-slate-600"><strong>Deliver to:</strong> {o.delivery_address}</p>}{["cancelled","rejected"].includes(o.status)&&<p className="mt-4 rounded-xl bg-slate-100 p-3 text-sm font-semibold">{labels[o.status]}</p>}<div className="mt-5 space-y-2 border-t pt-4">{(o.food_order_items||[]).map((i,n)=><div key={n} className="flex justify-between text-sm"><span>{i.quantity} × {i.item_name}</span><span>{o.currency} {Number(i.line_total).toLocaleString()}</span></div>)}</div>{o.status==="pending"&&<button disabled={busy===o.id} onClick={()=>cancel(o.id)} className="mt-5 rounded-full border px-4 py-2 text-sm font-semibold disabled:opacity-50">{busy===o.id?"Cancelling…":"Cancel order"}</button>}</article>})}{!orders.length&&!message&&<div className="rounded-3xl border border-dashed bg-white p-10 text-center text-slate-500">You have no restaurant orders yet.</div>}</div></div></main>
}
