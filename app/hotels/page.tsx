"use client";

import { FormEvent, useMemo, useState } from "react";
import Link from "next/link";

type Hotel = { provider: string; property_id: string; property_name: string; room_id: string | null; rate_id: string | null; currency: string; total: { amount: number; currency: string } | null; cancellation: string | null; availability: "available" | "unavailable" | "unknown"; };
function isoDate(offset: number) { const d = new Date(); d.setDate(d.getDate() + offset); return d.toISOString().slice(0, 10); }
export default function HotelsPage() {
  const [destination, setDestination] = useState(""); const [checkIn, setCheckIn] = useState(isoDate(7)); const [checkOut, setCheckOut] = useState(isoDate(9)); const [guests, setGuests] = useState("2"); const [rooms, setRooms] = useState("1"); const [results, setResults] = useState<Hotel[]>([]); const [loading, setLoading] = useState(false); const [error, setError] = useState("");
  const canSearch = useMemo(() => destination.trim() && checkIn && checkOut && checkOut > checkIn, [destination, checkIn, checkOut]);
  async function search(event: FormEvent) { event.preventDefault(); if (!canSearch) return; setLoading(true); setError(""); try { const params = new URLSearchParams({ destination: destination.trim(), check_in: checkIn, check_out: checkOut, guests, rooms, currency: "KES" }); const response = await fetch(`/api/v1/hotels?${params.toString()}`, { cache: "no-store" }); const body = await response.json().catch(() => null); if (!response.ok) throw new Error(body?.error?.message || body?.message || "Hotel inventory is temporarily unavailable."); setResults(Array.isArray(body?.data) ? body.data : Array.isArray(body) ? body : []); } catch (err) { setResults([]); setError(err instanceof Error ? err.message : "Hotel search failed."); } finally { setLoading(false); } }
  return <main className="min-h-screen bg-[#f7f7f4] text-[#111]"><section className="bg-[#111] text-white"><div className="mx-auto max-w-7xl px-6 pb-16 pt-12 sm:px-10 sm:pb-20 sm:pt-16"><Link href="/" className="text-xs font-semibold uppercase tracking-[.22em] text-white/45">SafariPlug</Link><div className="mt-12 max-w-3xl"><p className="text-[11px] font-semibold uppercase tracking-[.28em] text-white/45">Stay</p><h1 className="mt-4 text-5xl font-semibold tracking-[-.05em] sm:text-7xl">Find your stay.<br /><span className="text-white/40">Then keep it in your trip.</span></h1><p className="mt-6 max-w-2xl text-base leading-7 text-white/60 sm:text-lg">Search live hotel inventory by destination and dates. SafariPlug shows customer pricing in local currency while retaining the supplier quote for settlement.</p></div></div></section><section className="mx-auto max-w-7xl px-6 py-8 sm:px-10 sm:py-10"><form onSubmit={search} className="rounded-[2rem] bg-white p-5 shadow-[0_24px_80px_-50px_rgba(0,0,0,.5)] sm:p-7"><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-5"><label className="lg:col-span-2"><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[.16em] text-black/40">Destination</span><input value={destination} onChange={e => setDestination(e.target.value)} placeholder="Nairobi, Zanzibar, Kigali..." className="w-full rounded-2xl border border-black/10 bg-[#fafaf8] px-4 py-3.5 outline-none focus:border-black/30" /></label><label><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[.16em] text-black/40">Check in</span><input type="date" value={checkIn} onChange={e => setCheckIn(e.target.value)} className="w-full rounded-2xl border border-black/10 bg-[#fafaf8] px-4 py-3.5 outline-none" /></label><label><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[.16em] text-black/40">Check out</span><input type="date" value={checkOut} onChange={e => setCheckOut(e.target.value)} className="w-full rounded-2xl border border-black/10 bg-[#fafaf8] px-4 py-3.5 outline-none" /></label><label><span className="mb-2 block text-[11px] font-semibold uppercase tracking-[.16em] text-black/40">Guests / rooms</span><div className="flex gap-2"><input min="1" type="number" value={guests} onChange={e => setGuests(e.target.value)} className="w-1/2 rounded-2xl border border-black/10 bg-[#fafaf8] px-3 py-3.5 outline-none" /><input min="1" type="number" value={rooms} onChange={e => setRooms(e.target.value)} className="w-1/2 rounded-2xl border border-black/10 bg-[#fafaf8] px-3 py-3.5 outline-none" /></div></label></div><div className="mt-5 flex flex-wrap items-center gap-3"><button disabled={!canSearch || loading} className="rounded-full bg-black px-6 py-3 text-sm font-semibold text-white disabled:cursor-not-allowed disabled:opacity-40">{loading ? "Searching live inventory…" : "Search hotels"}</button></div></form>{error && <div className="mt-6 rounded-2xl border border-black/10 bg-white p-5 text-sm text-black/65">{error}</div>}<div className="mt-10 flex items-end justify-between gap-4"><div><p className="text-[11px] font-semibold uppercase tracking-[.2em] text-black/40">Results</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">{loading ? "Checking availability…" : results.length ? `${results.length} stays found` : "Ready when you are"}</h2></div>{results.length > 0 && <span className="text-xs text-black/40">{destination} · KES</span>}</div>{!results.length && !loading && !error && <div className="mt-6 rounded-[2rem] border border-dashed border-black/15 bg-white px-6 py-16 text-center"><p className="text-lg font-semibold">Search any destination.</p><p className="mx-auto mt-2 max-w-xl text-sm leading-6 text-black/50">SafariPlug is designed for open-ended destinations. Enter the city or place you actually want to visit rather than choosing from a fixed list.</p></div>}<div className="mt-6 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{results.map((hotel) => <article key={`${hotel.provider}-${hotel.property_id}-${hotel.room_id ?? "room"}`} className="rounded-[1.75rem] border border-black/8 bg-white p-5 shadow-[0_18px_60px_-45px_rgba(0,0,0,.45)]"><div className="flex aspect-[16/9] items-end rounded-[1.25rem] bg-gradient-to-br from-[#e7e2d7] via-[#d4cec0] to-[#aaa394] p-4"><span className="rounded-full bg-white/85 px-3 py-1.5 text-[10px] font-semibold uppercase tracking-[.15em]">{hotel.availability === "available" ? "Available" : "Live result"}</span></div><div className="pt-5"><div className="flex items-start justify-between gap-4"><h3 className="text-xl font-semibold tracking-tight">{hotel.property_name}</h3>{hotel.total && <p className="text-right text-lg font-semibold">{hotel.total.currency} {hotel.total.amount.toFixed(2)}</p>}</div><p className="mt-2 text-xs uppercase tracking-[.14em] text-black/35">{hotel.provider} · customer price</p>{hotel.cancellation && <p className="mt-4 text-sm leading-6 text-black/55">{hotel.cancellation}</p>}</div></article>)}</div></section></main>;
}
