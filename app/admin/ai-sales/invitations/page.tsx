import Link from "next/link";import {requireAdmin} from "@/lib/auth/require-admin";import {supabaseAdmin} from "@/lib/supabase-admin";import {createPartnerInvitation,draftPartnerInvitation,approvePartnerInvitation,sendPartnerInvitation} from "./actions";
const TYPES=["Hotel / stay","Restaurant / food","Driver / transfer","Tour / experience","Massage / wellness","Barber / grooming","Nails / beauty","Tattoo","Diving / watersports","Kitesurfing / instructor","Local","Other travel partner"];
export default async function Page(){await requireAdmin();const {data:rows}=await supabaseAdmin.from("partner_invitations").select("*").order("created_at",{ascending:false}).limit(100);return <main className="mx-auto max-w-6xl p-6 sm:p-10"><div className="flex items-center justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-black/40">AI Sales</p><h1 className="mt-2 text-3xl font-semibold">Partner Invitations</h1><p className="mt-2 text-sm text-black/55">Add an email or WhatsApp number. AI drafts; a person approves before anything is sent.</p></div><Link href="/admin/ai-sales" className="text-sm underline">Sales CRM</Link></div><form action={createPartnerInvitation} className="mt-8 grid gap-3 rounded-3xl border p-5 md:grid-cols-2"><input name="business_name" required placeholder="Business or partner name" className="rounded-xl border p-3"/><select name="partner_type" required className="rounded-xl border p-3"><option value="">Partner type</option>{TYPES.map(x=><option key={x}>{x}</option>)}</select><input name="contact_email" type="email" placeholder="Email address (optional)" className="rounded-xl border p-3"/><input name="whatsapp_phone" placeholder="WhatsApp number (optional)" className="rounded-xl border p-3"/><button className="rounded-xl bg-black px-5 py-3 font-semibold text-white md:col-span-2">Add partner invitation</button></form><section className="mt-8 space-y-4">{(rows||[]).map(i=><article key={i.id} className="rounded-3xl border bg-white p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-lg font-semibold">{i.business_name}</h2><p className="text-sm text-black/50">{i.partner_type} · {i.contact_email||i.whatsapp_phone} · {i.status}</p></div>{i.status==="draft"&&<form action={draftPartnerInvitation}><input type="hidden" name="id" value={i.id}/><button className="rounded-full bg-black px-4 py-2 text-sm text-white">Draft with AI</button></form>}</div>{i.ai_message&&<form action={approvePartnerInvitation} className="mt-4 space-y-3"><input type="hidden" name="id" value={i.id}/><input name="subject" defaultValue={i.ai_subject||""} className="w-full rounded-xl border p-3 font-medium"/><textarea name="message" defaultValue={i.ai_message} rows={9} className="w-full rounded-xl border p-3 text-sm"/>{i.status==="ready_for_approval"&&<button className="rounded-full border border-black px-4 py-2 text-sm font-semibold">Approve invitation</button>}</form>}{i.status==="approved"&&<form action={sendPartnerInvitation} className="mt-4"><input type="hidden" name="id" value={i.id}/><button className="rounded-full bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Send approved invitation</button><p className="mt-2 text-xs text-black/45">Email sends only through configured Resend. WhatsApp-only invitations stay unsent until a real WhatsApp Business provider is connected.</p></form>}{i.status==="sent"&&<p className="mt-4 text-sm font-medium text-emerald-700">Invitation sent. Signup and onboarding progress will be tracked from the invitation link.</p>}</article>)}</section></main>}
